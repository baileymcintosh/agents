name: Builder Agent

on:
  # Run after planner completes (workflow chaining via workflow_run)
  workflow_run:
    workflows: ["Planner Agent"]
    types: [completed]
  # Manual trigger
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (no Claude calls)"
        required: false
        default: "false"
        type: choice
        options: ["true", "false"]

jobs:
  build:
    name: Execute Research Tasks
    runs-on: ubuntu-latest
    # Only run if planner succeeded (or if manually triggered)
    if: >
      github.event_name == 'workflow_dispatch' ||
      github.event.workflow_run.conclusion == 'success'
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # Pull latest to get the planner's committed report
          ref: main

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Set up Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync

      - name: Run Builder Agent
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_ENGINEERING_CHANNEL_ID: ${{ secrets.SLACK_ENGINEERING_CHANNEL_ID }}
          AGENT_MODEL: ${{ vars.AGENT_MODEL || 'claude-opus-4-6' }}
        run: |
          DRY_RUN_FLAG=""
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            DRY_RUN_FLAG="--dry-run"
          fi
          uv run agentorg run builder $DRY_RUN_FLAG

      - name: Commit and push reports
        if: github.event.inputs.dry_run != 'true'
        run: |
          git config user.name "AgentOrg Bot"
          git config user.email "agentorg-bot@users.noreply.github.com"
          git add reports/
          git diff --staged --quiet || git commit -m "chore: builder report $(date -u +%Y-%m-%d)"
          git push
